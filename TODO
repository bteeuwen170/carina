 TODO
======
General (AKA angry notes to self)
- No typdeffing for kernelspace structures
- No string typedef, WTF
- RESERVED IS ___NOT___ THE SAME AS UNUSED!!!
- Create devel branch once basic posix things are implemented like vfs and pipes

Bootloader
- Write own bootloader

Kernel
- Fix unable to fit without stripping kernel
. Finish PCI driver
. Finish AC97 driver
- Finish ext2 driver
. Finish ATA/ATAPI driver
- Scrollbuffer 64KB size
- Check mboot flags before using them
- deregister -> unregister (WTF)
- Recursive kprintf
- Make #includes look nicer
- Use bool in some places instead of i32 or char or u8 or i8
- Move sys directory
- NO ITOA
- Typedef certain file types like uid_t and gid_t
- Write parser for pci.ids file
- Script to get latest pci.ids
- Always return void from initialization functions
n Native support for MS-DOS executables using some kind of low-level vm,
  if implementable...
- Move paging source file to cpu/
- Don't include arch specific files outside of arch dir
- Use sse + sse2 on > x86_64 and use mmx (+ sse) or sse + sse2 on i686
- Inconsistency between use of kernel integer data types and
  C integer data types
- base_adr_* to bar_*
- Seperate all driver header files into it's own directory
- FIXME PCI bug, handler isn't called after reboot (dirty memory)
- Don't allow including files from dev/, provide interface in include/dev/
  instead
- Linked list generic solution
- Scroll buffer with shortcuts to move up and down

Makefile
- Seperate kernel-o and kernel-l (objects and libs)
-   CC       .

OCD
- Remove the extra blank line after the header
- No space between "asm volatile" and "("
- Don't use "unused*" but instead just mention that the field is unused in
		a comment

Toolchain
- Libc
- Self contained autoconf and automake
- Put up precompiled version
- i686 libgcc broken


 Legend
========
- = TODO
! = Important TODO
. = In progress
? = Probably done
x = Done
n = Cancelled
p = Postponed

> = Sidenote
